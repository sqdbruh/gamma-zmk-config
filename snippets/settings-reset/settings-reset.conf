# Settings-reset firmware: flash once to wipe BLE bonds and the ZMK
# settings store, then reflash the regular firmware. Used to re-pair
# halves with the dongle from a clean state.
CONFIG_ZMK_SETTINGS_RESET_ON_START=y
CONFIG_ZMK_BLE_CLEAR_BONDS_ON_START=y
