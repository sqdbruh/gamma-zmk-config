# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property. You can also use the `cmake-args` property to
# pass flags to the build command, `snippet` to add a Zephyr snippet, and
# `artifact-name` to assign a name to distinguish build outputs from each other:
#
# board: [ "nice_nano" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano
#     shield: reviung41
#   - board: nice_nano
#     shield: corne_left
#     snippet: studio-rpc-usb-uart
#     cmake-args: -DCONFIG_ZMK_STUDIO=y
#     artifact-name: corne_left_with_studio
#
---
include:
  - board: gamma_left
  - board: gamma_right
  - board: gamma_left
    cmake-args: -DCONFIG_GAMMA_LED_TEST=y
    artifact-name: gamma_left_led_test
  - board: gamma_right
    cmake-args: -DCONFIG_GAMMA_LED_TEST=y
    artifact-name: gamma_right_led_test
  - board: gamma_left
    cmake-args: -DCONFIG_GAMMA_LED_PROBE=y -DCONFIG_SPI=n -DCONFIG_LED_STRIP=n -DCONFIG_WS2812_STRIP_SPI=n -DCONFIG_I2C=n -DCONFIG_SENSOR=n
    artifact-name: gamma_left_led_probe
  - board: gamma_right
    cmake-args: -DCONFIG_GAMMA_LED_PROBE=y -DCONFIG_SPI=n -DCONFIG_LED_STRIP=n -DCONFIG_WS2812_STRIP_SPI=n -DCONFIG_I2C=n -DCONFIG_SENSOR=n
    artifact-name: gamma_right_led_probe
  - board: gamma_dongle
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y
  - board: gamma_left
    shield: settings_reset
    artifact-name: gamma_left_settings_reset
  - board: gamma_right
    shield: settings_reset
    artifact-name: gamma_right_settings_reset
  - board: gamma_dongle
    shield: settings_reset
    artifact-name: gamma_dongle_settings_reset
