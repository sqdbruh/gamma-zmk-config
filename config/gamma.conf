# Place keyboard-wide overrides here. Per-board defaults live in
# boards/arm/gamma/gamma_<variant>_defconfig and are applied automatically.

# Example: enable extra USB logging on all variants
# CONFIG_ZMK_USB_LOGGING=y
